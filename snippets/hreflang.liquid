{% liquid
  assign path = request.path | remove: '/en-ca' | remove: '/en-au' | remove: '/de-de' | remove: '/fr-fr' | replace: '/collections/under-25', '/collections/under-15' | replace: '/collections/under-130', '/collections/under-100' | replace: '/pages/cliphair-bridal-hair-extensions', '/pages/2023-bridal-hairstylist-award'
%}

<link
  rel="alternate"
  hreflang="x-default"
  href="{{ shop.url | replace: shop.domain, 'www.cliphair.co.uk' | append: path  }}"
>
<link
  rel="alternate"
  href="{{ shop.url | replace: shop.domain, 'www.cliphair.co.uk' | append: path  }}"
  hreflang="en-GB"
>
<link
  rel="alternate"
  href="{{ shop.url | replace: shop.domain, 'www.cliphair.co.uk/el-cy' | append: path  }}"
  hreflang="el-CY"
>
<link
  rel="alternate"
  href="{{ shop.url | replace: shop.domain, 'www.cliphair.co.uk/fi-fi' | append: path  }}"
  hreflang="fi-FI"
>
<link
  rel="alternate"
  href="{{ shop.url | replace: shop.domain, 'www.cliphair.co.uk/no-no' | append: path  }}"
  hreflang="no-NO"
>
